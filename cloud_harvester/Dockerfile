FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends     curl     libgl1     libglib2.0-0     sqlite3     && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY standalone_cloud_harvester.py .

VOLUME ["/app/harvested_data"]

CMD ["python", "standalone_cloud_harvester.py"]
